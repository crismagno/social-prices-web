namespace EmployeesEnum {
  export enum Level {
    ADMIN = "ADMIN",
    MASTER = "MASTER",
    EMPLOYEE = "EMPLOYEE",
  }

  export enum Status {
    PENDING = "PENDING",
    ACTIVE = "ACTIVE",
    STOPPED = "STOPPED",
  }
}

export default EmployeesEnum;
