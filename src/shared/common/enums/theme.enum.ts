namespace ThemeEnum {
  export enum Theme {
    LIGHT = "light",
    DARK = "dark",
  }

  export const ThemeLabels = {
    [Theme.LIGHT]: "Light",
    [Theme.DARK]: "Dark",
  };
}

export default ThemeEnum;
